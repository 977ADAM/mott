# Mott — спецификация проекта

> Дообучение Qwen3-4B на чеченском языке. Цель — инструктивная модель, которая говорит
> на чеченском естественно и грамотно.

Статус: черновик v0.3. Всё, что помечено **[проверено]**, сверено с Hugging Face API,
а не взято на веру. Пункты **[TBD]** требуют решения автора.

---

## 1. Цель и границы

**Цель:** инструктивная модель на базе Qwen3-4B, обученная через QLoRA локально
(MLX-LM) на чеченских данных, которая:

1. Корректно отвечает по-чеченски на чеченские и русские запросы.
2. Грамотно использует чеченскую орфографию (палочку `Ӏ` и диграфы `аь`, `оь`, `уь`,
   `юь`, `яь`, `гӏ`, `кӏ`, `пӏ`, `тӏ`, `хӏ`, `цӏ`, `чӏ`, `кх`, `къ`, `хь`).
3. Порожает осмысленный чеченский текст, а не кальки с русского.

**Вне границ:** pretraining с нуля; распределённое обучение; tool-use/агенты;
полное покрытие языка на v0.1.

### 1.1 Измеренное окружение [проверено]

| Параметр | Значение |
|---|---|
| Модель | MacBook Pro, идентификатор `Mac17,2` |
| Чип | Apple M5: 10 ядер CPU (4P + 6E), 10 ядер GPU |
| Память | 24 ГБ объединённой (`hw.memsize` = 25 769 803 776 байт) |
| **Бюджет памяти под MLX** | **17,76 ГБ** (`max_recommended_working_set_size`) |
| ОС | macOS 26.6.2 (25G83) |
| MLX (core) | 0.32.0 — установлен системно |
| `mlx-lm` | не установлен; ставим в venv проекта |
| Python | системный 3.14.7; проект пинует 3.13 (`.python-version`), `uv` есть |
| Диск | 689 ГБ свободно |

**Лимит под тензоры — 17,8 ГБ, а не 24 ГБ.** Остаток macOS держит под систему.
`iogpu.wired_limit_mb` не поднимаем: нужен `sudo`, выигрыш не стоит риска.

---

## 2. Базовая модель [проверено]

`mlx-community/Qwen3-4B-4bit` — существует, 20 557 загрузок, 16 лайков,
последнее изменение 28.04.2025.

| Свойство | Значение |
|---|---|
| `model_type` | `qwen3` |
| Слоёв (`num_hidden_layers`) | **36** |
| `hidden_size` | 2560 |
| Головы | 32 attention / 8 KV (GQA) |
| `max_position_embeddings` | 40 960 (мы ограничим 2048) |
| `tie_word_embeddings` | true |
| Квантование | **4 бита, group_size 64** |
| `eos_token` | `<|im_end|>` |
| `pad_token` | `<|endoftext|>` |
| Файл весов | `model.safetensors` (шардированный) |

Следствия для обучения:

- **QLoRA включается автоматически** — модель уже квантована, доп. флаги не нужны.
- `--num-layers 16` = 16 верхних слоёв из 36. Полный охват — `--num-layers 36`.
- Веса ≈ 2,4 ГБ → в бюджет 17,8 ГБ влезаем свободно.

### 2.1 Поведение chat-шаблона Qwen3 [проверено]

Шаблон в `tokenizer_config.json` содержит переменную `enable_thinking`. Разбираем, что
это значит **для обучения**:

- Ветка `add_generation_prompt` (генерация) при `enable_thinking=false` дописывает
  `<think>\n\n</think>\n\n` — это «режим без размышлений» Qwen3.
- В ветке **обучения** (`add_generation_prompt=False`) для последнего assistant-сообщения
  шаблон сам оборачивает ответ: `<|im_start|>assistant\n<think>\n{reasoning}\n</think>\n\n{content}`.

Практические выводы — это важно и легко испортить:

1. **`<think>` в `content` писать НЕ надо.** Пустой блок подставит сам шаблон.
2. **`reasoning_content` не нужен** — обучаем в non-thinking режиме, это то, что мы хотим
   от языковой модели для чеченского.
3. **В `content` не должно быть подстроки `</think>`** — шаблон по ней режет сообщение.
4. **`system`-сообщение обязано быть первым в списке** — шаблон проверяет `messages[0]`.
5. При инференсе через `mlx_lm.generate` шаблон применяется с теми же правилами, поэтому
   train и inference согласованы.

**Проверено эмпирически** (`uv run python scripts/check_tokens.py`, секция 1): рендер
учебного примера действительно содержит пустой блок `<think>\n\n</think>`, а промпт
генерации с `enable_thinking=False` даёт **тот же самый префикс**. То есть train и
inference согласованы, и специально размечать размышления в данных не нужно.

Итоговый JSONL остаётся чистым:

```jsonl
{"messages": [{"role": "user", "content": "..."}, {"role": "assistant", "content": "..."}]}
```

---

## 3. Данные

### 3.1 Формат

```
data/
├── train.jsonl   # обязательно
├── valid.jsonl   # опционально; включаем — нужен валидационный loss
└── test.jsonl    # для mlx_lm.lora --test
```

Один JSON-объект на строку, без переносов внутри объекта:

```jsonl
{"messages": [{"role": "user", "content": "Муха хьо?"}, {"role": "assistant", "content": "Дика ву, баркалла."}]}
```

> **Важно:** весь чеченский текст (включая `system`) финализирует носитель.
> Примеры в этой спеке — плейсхолдеры для иллюстрации формата.

### 3.2 Объём

| Файл | Объём | Назначение |
|---|---|---|
| `train.jsonl` | 500–1000 | обучение |
| `valid.jsonl` | 50–100 (~10 %) | валидационный loss |
| `test.jsonl` | 50–100, зафиксировать | финальная перплексия |

### 3.3 Инвентарь источников [проверено]

Всё измерено через Hugging Face API.

| Датасет | Строк | Поля | Что это на самом деле |
|---|---|---|---|
| `NM-development/nmd-ce-ru-171k-v0` (config `default`) | 171 224 | `ce`, `ru`, `source` | **преимущественно словарь** |
| `NM-development/nmd-ce-ru-171k-v0` (config `benchmark`) | 360 | `ce`, `ru`, `en`, `source` | словарь + стихи Библии |
| `NM-development/wmt24pp-ce` | 998 | `lp`, `domain`, `document_id`, `segment_id`, `is_bad_source`, `source`, `target`, `original_target` | новостные en→ce предложения |
| `NM-development/ce_ru_officials` | 66 | `ce_text`, `ru_text`, `source` | официальные тексты ce↔ru |
| `NM-development/ce_ru_toponyms` | 344 | `ce_text`, `ru_text`, `source` | топонимы ce↔ru |
| `NM-development/smol_ce` | 825 | `id`, `paragraph_id`, `intraparagraph_id`, `che_Cyrl`, `rus_Cyrl`, `eng_Latn` | **выровненный трёхъязычный корпус** |
| `NM-development/chechen_given_names` (+ `_male`/`_female`/`_bessmertnybarak_v0`) | — | — | личные имена |

> **Имена полей различаются!** `ce`/`ru` — только у `nmd-ce-ru-171k-v0`. У
> `ce_ru_officials` и `ce_ru_toponyms` это `ce_text`/`ru_text`, у `smol_ce` —
> `che_Cyrl`/`rus_Cyrl`/`eng_Latn`. Не хардкодить `ce`/`ru` в конструкторе датасета.
>
> `smol_ce` оказался полезнее, чем выглядел: это 825 предложений, **выровненных сразу
> по трём языкам** (ce/ru/en) — из него можно делать и перевод, и кросс-язычные задачи.

Всего у `NM-development` **9 датасетов**.

#### ⚠ Разбор `nmd-ce-ru-171k-v0` — читать до планирования

Выборка 400 строк по всему файлу:

| Метрика | Значение |
|---|---|
| Медиана слов в `ce` | **2** |
| Среднее | 7,3 |
| p90 | 25 |
| Максимум | 63 |
| Доля строк из 1 слова | **40 %** |
| Доля строк из 2 слов | 25 % |
| Доля строк > 5 слов | 26 % |
| Топ-источник | «Chechen-Russian dictionary, A. G. Matsiev» — **300 из 400 строк** |

**Вывод: это словарь, а не корпус предложений.** 171 224 строки выглядят огромным
объёмом, но ~75 % — отдельные слова и словосочетания вида
`туьркан дитт → абрикосовое дерево`.

**Что это значит практически:** если скормить этот датасет в chat-формате как есть,
модель обучится отвечать односложными обрывками. Это прямая дорога к модели, которая на
любой вопрос выдаёт 1–2 слова. Поэтому:

- Использовать для обучения — **только предложенческую часть** (> 5 слов, ~26 %).
- Словную часть — отложить на потом (задача «перевод термина») или не использовать на v0.1.
- Стихи Библии — отдельный архаичный регистр, смешивать с бытовым чеченским осторожно.

#### ⚠ `google/wmt24pp` — чеченского в нём НЕТ [проверено]

Проверены все **55** конфигов `google/wmt24pp` — это пары `en-XX_YY`
(`en-ru_RU`, `en-de_DE`, …). **Ни одного конфига с чеченским.** Совпадений по `ce` — ноль.

Чеченский материал WMT24++ лежит в `NM-development/wmt24pp-ce` (998 строк,
`lp = en-ce_CE`, домен `news`). То есть источник правильный, но имя датасета другое.

В этом файле есть две ловушки:

- **Строка-canary** (`domain = canary`, `is_bad_source = true`) — служебная, выкинуть.
- Флаг **`is_bad_source`** — фильтровать по нему.

### 3.3.1 Воронка фильтрации [проверено]

Замерено скриптом `scripts/filter_dictionary.py` на `nmd-ce-ru-171k-v0`:

| Шаг | Осталось | Δ |
|---|---|---|
| вход | 171 224 | — |
| `ce` длиннее 5 слов | 65 488 | −105 736 |
| минус религиозные (Библия + Коран) | **3 220** | −62 268 |
| минус `num2words` | **1 850** | −1 370 |
| минус пустые и дубли по `ce` | **1 840** | −10 |

**Итог решения «фильтровать религию»: пул сжимается до 1 840 строк**, и это ещё не
означает 1 840 предложений. Состав оставшегося:

| Строк | Что это |
|---|---|
| 930 | фразы из словаря Мациева (идиомы/коллокации — не проза) |
| 174 | словарь компьютерной лексики |
| **172** | **«Воспоминания об абреке Зелимхане», Муса Бакаров — проза** |
| 62 | словарь Берсанова |
| **59** | **«Долгие ночи», Абузар Айдамиров — проза** |
| 44 | административные названия (daymohk-gazet) |
| ~150 | Radio Marsho — новостные предложения |

То есть **настоящей прозы ≈ 400 строк**, остальное — словарь и администрирование.

**Для сравнения, если религию НЕ фильтровать:** после `num2words` и дедупа по `ce`
остаётся **41 069 строк** (медиана 19 слов). Дедуп убирает 23 049 строк, потому что Коран
в этом датасете — **шестикратно параллельный**: переводы Магомедова и Ибрагимова, каждый
в трёх русских редакциях (Адель, Кулиев, Османов). Дедуп по `ce` схлопывает шесть
вариантов в два.

Итого цена решения: **1 840 строк против 41 069**. Это в 22 раза меньше.

> **Поправка к более раннему прогнозу.** Раньше в этом документе говорилось, что
> предложенческая часть «сожмётся до ~2–3 тысяч пар». Формально цифра похожа (3 220),
> но состав иной: 42 % этого остатка — `num2words`, а прозы всего ~400 строк.
> Прежняя оценка вводила в заблуждение, и решение принималось на её основе.

#### Разметка религиозных источников [проверено, с исправлением]

Замер артефактов в чеченской части:

| Группа | Строк | Содержит `"` | Двойные пробелы |
|---|---|---|---|
| Библия | 28 669 | 28,3 % | 1,8 % |
| Коран | 37 323 | 51,1 % | **64,5 %** |
| прочее (словари) | 105 232 | 0,1 % | 1,3 % |

> **Исправление.** В первой редакции здесь утверждалось, что `"` в Коране —
> «повреждение текста, потеря знака при конвертации». **Это неверно.** Разбор показал,
> что `"` — обычная кавычка:
>
> - `цара элира: "Дайъа цунах тешначеран божабераш..."` — прямая речь;
> - `"Хьарам" маьждигера "Акъса" маьждиге` — кавычки вокруг названий;
> - непарные числа объясняются **разбиением по аятам**: речь открывается в одном стихе
>   и закрывается в другом. Из 5 912 строк ровно с одной кавычкой 1 028 — открытие
>   после `:`, 1 007 — закрытие в конце строки.

Реальный артефакт — только пробелы: 64,5 % строк Корана с двойными пробелами,
8,7 % с пробелом перед запятой, 10,4 % перед точкой. Чистится безопасно (`mott/text.py`),
после чистки все три показателя — 0,0 %.

Пробелы внутри кавычек (38,0 % / 26,1 %) **сознательно не чинятся**: отличить открывающую
кавычку от закрывающей в этой разметке нельзя — строки часто начинаются с закрывающей
(речь из предыдущего аята). Обе попытки это сделать ломали законную пунктуацию:
`элира: "Ас...` → `элира:"Ас...` и `дира: " Цуьнан...` → `дира:" Цуьнан...`.

### 3.4 Отсутствующее звено: инструктивных данных на чеченском нет

Ни в одном из 9 датасетов нет пар вида «инструкция → ответ» на чеченском. Всё, что есть —
параллельные тексты и словарь. Значит, **инструктивный датасет придётся конструировать**,
а не скачивать. Это главный риск проекта, и он определяет план работ.

Стратегия — от проверяемого к непроверяемому:

1. **Перевод ru↔ce** из предложенческой части параллельных данных. Носитель может
   проверить качество построчно. Составляет основную массу `train.jsonl`.
2. **Перевод терминов** (`ce_ru_toponyms`, `ce_ru_officials`, имена) — узкая, но
   безошибочная задача.
3. **Диалог/инструкции, написанные носителем вручную** — 100–200 пар. Только так модель
   получит поведение «отвечай на вопрос», а не «переведи строку».
4. Синтетика из сырого чеченского текста (саммаризация/QA) — **после** того, как есть чем
   валидировать; обязательно ручная фильтрация носителем.

> Осторожно с синтетикой: если генерировать чеченские инструкции слабой моделью, ошибки
> её чеченского попадут в датасет. Проверка носителем обязательна.

#### Реальный бюджет перевода после фильтра (§3.3.1)

| Источник | Пригодных строк | Регистр |
|---|---|---|
| `smol_ce` | 825 | разговорный (ce/ru/en) |
| `wmt24pp-ce` | 960 | новости/соцсети/литература, **en→ce, русского нет** |
| проза из `nmd_171k` | ~400 | литература + новости |
| `ce_ru_toponyms` + `ce_ru_officials` | 410 | термины/официальное |

**Для задачи ru↔ce доступно ≈ 1 635 пар** (без `wmt24pp-ce`, там английский источник).
Целевые 500 пар обучения берут треть этого запаса, и на тест остаётся мало. Это узкое
место плана, а не деталь.

### 3.5 Риск утечки

`benchmark`-сплит из `nmd-ce-ru-171k-v0` (360 строк) — отдельный конфиг, но его содержимое
может пересекаться с `default`. **Дедуплицировать по нормализованному `ce` перед сплитом**,
иначе тест покажет завышенный результат.

### 3.6 Чек-лист качества на каждый пример

- [ ] Чеченский текст написан/проверен носителем.
- [ ] Палочка `Ӏ` (`U+04C0`) / `ӏ` (`U+04CF`) на месте, диграфы корректны.
- [ ] Валидный JSON, один объект на строку, ключи `role`/`content`.
- [ ] В `content` нет подстроки `</think>`.
- [ ] `system` (если есть) — первый в списке `messages`.
- [ ] Нет дублей и почти-дублей между train и test.
- [ ] Ответ — то, что мы хотим от модели (не обрезан, не пустой).

---

## 4. Обучение

### 4.1 Установка

```shell
uv venv --python 3.13
uv pip install "mlx-lm[train]==0.31.3"
```

### 4.2 Рабочий запуск

Проверенный конфиг (обоснование каждого параметра — в §4.3 и §4.4):

```shell
mlx_lm.lora \
  --model mlx-community/Qwen3-4B-4bit \
  --train \
  --data ./data \
  --batch-size 2 \
  --num-layers 16 \
  --learning-rate 1e-5 \
  --iters 1800 \
  --max-seq-length 512 \
  --mask-prompt \
  --steps-per-eval 150 \
  --steps-per-report 50 \
  --val-batches 25 \
  --save-every 300 \
  --seed 42 \
  --adapter-path ./adapters/mott-v0.1
```

`--iters 1800` = 3 эпохи по 1 197 примеров при батче 2 (`итераций = эпохи × N / batch`).

### 4.3 Гиперпараметры [проверено]

Замерено на реальном `data/train.jsonl` (1 200 примеров) и дымовом прогоне:

| Метрика | Значение |
|---|---|
| Токенов в train | 140 763 |
| Токенов на пример | медиана 98, среднее 117, p90 210, **max 680** |
| Токенов на ответе (под loss) | медиана 35, среднее 47 |
| Обучаемых параметров LoRA | **7,34 млн (0,182 %** от 4 022 млн) |
| Пик памяти при `--batch-size 2` | **5,51 ГБ** из 17,76 ГБ бюджета |

Следствия:

- **`--max-seq-length 1024` достаточно**, а не 2048: самый长 пример — 680 токенов (33 % от
  2048). Обрезки нет ни на одном примере.
- **`--mask-prompt` считает loss всего на 35 токенах из 98** — учим ответ, а не промпт.
  Это правильное поведение, но оно означает, что полезный сигнал составляет ~40 % объёма.
- **Памяти троекратный запас** (5,5 из 17,8 ГБ), поэтому батч можно держать большим:
  он даёт лучшую загрузку GPU без риска.

| Параметр | Рабочее значение | Замечание |
|---|---|---|
| `--batch-size` | 8 | при OOM/свопе → 4, затем 2 |
| `--num-layers` | 16 | из 36 слоёв; 36 = все, дороже |
| `--learning-rate` | 1e-5 | безопасный старт на малых данных |
| `--iters` | 450 | при батче 8 это 3 эпохи по 150 итераций |
| `--max-seq-length` | 1024 | max пример — 680 токенов, запаса хватает |
| `--mask-prompt` | вкл | loss только на ответе |
| `--grad-checkpoint` | выкл | не понадобился: памяти хватает |

**Как считать итерации.** 1 эпоха = 1 200 примеров. При `--batch-size B`:
`итераций на эпоху = 1200 / B`. Для 3 эпох: батч 8 → 450, батч 4 → 900, батч 2 → 1 800.
`--grad-accumulation-steps N` увеличивает эффективный батч в N раз, не тратя память,
но итераций нужно во столько же раз меньше.

**Каскад при нехватке памяти:** закрыть браузер → `--batch-size 4` → `--grad-checkpoint` →
`--batch-size 2` → `--num-layers 8`.

### 4.4 Память и паддинг — главная ловушка [проверено]

**Батч 8 уронил обучение по памяти.** Это самая дорогая ошибка в проекте, поэтому
фиксируем механику.

`mlx_lm/tuner/trainer.py` паддит **весь батч до самого длинного примера в нём**:

```python
pad_to = 32
max_length_in_batch = 1 + pad_to * ((max(lengths) + pad_to - 1) // pad_to)
max_length_in_batch = min(max_length_in_batch, max_seq_length)
```

Следствия:

1. **`max_seq_length` — не бюджет, а только потолок.** Если в батче есть пример на
   680 токенов, все остальные добиваются до 680, сколько бы ни было `max_seq_length`.
2. **Разброс длин дороже среднего размера.** У нас медиана 98, p90 210, max 490.
   Батч из 8 коротких примеров обрабатывает 8 × ~214 позиций, а полезных — ~103 × 8.
   Замер: **паддинг раздувает батч в 2,18×**.
3. **Сортировать данные бесполезно:** тренер перемешивает батчи
   (`np.random.permutation(len(batch_idx))`), порядок в файле ни на что не влияет.
4. **Больший батч не ускоряет обучение.** Замер: батч 2 давал 1,3 итер/с (1,5 примера/с),
   батч 8 — 0,18 итер/с (1,44 примера/с). Скорость та же, а памяти втрое больше.

**Модель памяти.** Замерено две точки: батч 2 → пик 5,51 ГБ, батч 8 → пик 17,755 ГБ
(и OOM). Отсюда **≈10 МБ на позицию токена** (позиция = строка × длина батча).
Бюджет 17,76 ГБ минус веса 2,4 ГБ минус служебное ≈ **14 ГБ на активации ⇒ ~1 400 позиций**.

Худший батч = `batch_size × максимальная длина в данных`:

| `--batch-size` | Худший батч (max 490) | Активации | Вердикт |
|---|---|---|---|
| 2 | 980 | ~9,8 ГБ | ✅ безопасно |
| 3 | 1 470 | ~14,7 ГБ | ⚠️ впритык |
| 4 | 1 960 | ~19,6 ГБ | ❌ OOM |
| 8 | 3 920 | ~39 ГБ | ❌ OOM (проверено) |

**Отсюда правило: длинные примеры выгоднее отсечь, чем дать `mlx-lm` обрезать их молча.**
`scripts/build_dataset.py --max-tokens 512` выкидывает такие примеры (у нас — 3 из 1 420)
и печатает оценку худшего батча. Обрезанный ответ учил бы модель обрывать фразу.

### 4.5 Что `mlx_lm.lora` делает сам

- Берёт `train.jsonl` (обязательно) и `valid.jsonl` (если есть) из `--data`.
- Определяет формат `chat` по ключу `messages`, применяет chat-шаблон Qwen3.
- Квантованная база → QLoRA без доп. флагов.
- Сохраняет адаптер в `--adapter-path`.

---

## 5. Оценка

### 5.1 Перплексия

```shell
mlx_lm.lora --model mlx-community/Qwen3-4B-4bit \
  --adapter-path ./adapters/mott-v0.1 --data ./data --test
```

Сравниваем база без адаптера → с адаптером. Цель — снижение на чеченском тесте.

### 5.2 Ручная оценка (главная метрика)

30–50 новых промптов, не из train/valid/test:

| Критерий | Шкала |
|---|---|
| Грамматическая правильность | 1–5 |
| Орфография (палочка, диграфы) | 1–5 |
| Естественность (не калька с русского) | 1–5 |
| Соответствие запросу | 1–5 |

Порог для v0.1: средний ≥ 3.5 на ≥ 70 % ответов.

### 5.3 Генерация

```shell
mlx_lm.generate --model mlx-community/Qwen3-4B-4bit \
  --adapter-path ./adapters/mott-v0.1 --prompt "Муха хьо?"
```

---

## 6. Токенизация [проверено]

Qwen3 — byte-level BPE (`vocab_size` = 151 936). Замерено скриптом
`scripts/check_tokens.py` на 3 000 чеченских и 3 000 русских предложениях
(длиннее 6 слов, из `nmd-ce-ru-171k-v0`) и 998 английских новостных строках:

| набор | симв/токен | токенов/символ | **токенов/слово** | vs ru |
|---|---|---|---|---|
| en (WMT24++ news) | 4,39 | 0,228 | 1,30 | 0,61× |
| **ce (предложения)** | **1,69** | **0,593** | **3,61** | **1,58×** |
| ru (предложения) | 2,67 | 0,374 | 2,11 | 1,00× |

**Вердикт: чеченский стоит в 1,58× больше токенов на символ, чем русский**, и
**3,61 токена на слово** против 2,11 у русского. Это заметное раздутие.

### 6.1 Причина — не палочка

Замер отдельных знаков опровергает исходную гипотезу:

- Палочка `Ӏ` (`U+04C0`) и `ӏ` (`U+04CF`) кодируются **одним токеном каждая** — символ
  токенизатору знаком.
- А вот **ни один** из 16 проверенных диграфов (`аь`, `оь`, `уь`, `юь`, `яь`, `гӏ`, `кӏ`,
  `пӏ`, `тӏ`, `хӏ`, `цӏ`, `чӏ`, `кх`, `къ`, `хь` и др.) не является единицей словаря BPE:
  каждый стоит **2 токена**.

То есть проблема системная — чеченская кириллица с плотным использованием диграфов плохо
представлена в BPE-словаре, а не «палочка ломает токенизацию».

### 6.2 Что из этого следует

- **Эффективный контекст в словах для чеченского примерно вдвое меньше**, чем для русского:
  при `--max-seq-length 2048` это ≈ 567 чеченских слов против ≈ 970 русских.
- Для **обучения** ограничение не мешает: p90 чеченского предложения в корпусе — 25 слов
  (≈ 90 токенов), то есть 2048 с большим запасом.
- Ограничение ударит при **длинной генерации** на инференсе — там контекст в словах
  окажется вдвое короче ожидаемого.
- Токенизатор **не переучиваем**: дорого, требует пересборки эмбеддингов и на 500–1000
  примерах выигрыша не даст.
- Раздутие надо просто учитывать в бюджете токенов и времени обучения.

---

## 7. Структура репозитория (цель)

```
mott/
├── SPEC.md
├── README.md
├── pyproject.toml           # uv, Python 3.13 (починить entry point mott:main)
├── data/
│   ├── raw/                 # скачанные HF-датасеты
│   ├── train.jsonl
│   ├── valid.jsonl
│   └── test.jsonl
├── configs/lora.yaml
├── scripts/
│   ├── download_data.py     # скачивание NM-development датасетов
│   ├── build_dataset.py     # конструктор chat-пар из параллельных данных
│   ├── filter_dictionary.py # отсев словной части 171k
│   ├── split_data.py        # сплит + дедуп против утечки
│   ├── check_tokens.py      # токены/символы ce vs ru
│   └── evaluate.py
├── adapters/mott-v0.1/
└── mott/                    # пакет в корне; uv_build: module-root = ""
```

> Раскладка — **root layout** (`mott/` рядом с `pyproject.toml`), не `src/`.
> Поэтому в `pyproject.toml` задан `[tool.uv.build-backend] module-root = ""`:
> по умолчанию `uv_build` ищет `src/<name>/` и падает с
> «Expected a Python module at: src/mott/__init__.py».

---

## 8. Этапы

| # | Этап | Результат | Критерий готовности | Статус |
|---|---|---|---|---|
| 1 | Скачать данные | `data/raw/` | 6 датасетов на диске | ✅ готово (17,4 МБ) |
| 2 | Замер токенизации | `scripts/check_tokens.py` | цифры ce vs ru | ✅ готово (1,58×) |
| 3 | Фильтрация | `data/pool_nmd.parquet` | пул предложений | ✅ готово (7 492) |
| 4 | Конструктор датасета | `train.jsonl` | формат chat, нет `</think>` | ✅ готово (1 200) |
| 5 | Сплит | `valid.jsonl`, `test.jsonl` | дедуп по `ce` против утечки | ✅ готово (120 / 100) |
| 6 | Окружение | venv + `mlx-lm[train]` | `mlx_lm.lora --help` работает | ✅ готово |
| 7 | Обучение | `adapters/mott-v0.1` | valid loss падает, нет NaN | ✅ прошло (переобучение после 1200) |
| 8 | Оценка | перплексия + рубрика | ниже baseline | ⚠️ ppl 159,9→23,2, но генерация сломана (§11) |
| **A** | **Языковая адаптация** | LoRA на сыром чеченском тексте | ppl ce 41,5 → ~12 | ⬜ **новый приоритет** |
| 9 | Итерация | список ошибок → правки данных | задокументировано | ⬜ после стадии A |

---

## 9. Риски

| Риск | Ответ |
|---|---|
| **Нет инструктивных данных на чеченском** | конструируем из параллельных + рукописные пары носителя (§3.4) |
| **171k — это словарь, не корпус** | берём только часть >5 слов; словную откладываем |
| Упор в лимит 17,8 ГБ / своп | каскад из §4.3; закрыть браузер |
| Переобучение на малых данных | LR 1e-5, `--mask-prompt`, валидационный loss |
| Забывание русского / базовых навыков | LoRA на 16 верхних слоях, малый LR; проверить русский промпт |
| Неэффективная токенизация ce | замер до старта (§6) |
| Утечка train/test | дедуп по нормализованному `ce` (§3.5) |
| Ошибки орфографии | чек-лист §3.6 + ручная оценка как обязательный гейт |
| Дрейф версий `mlx-lm` | версия пинуется в `pyproject.toml` |

---

## 10. Критерии успеха v0.1

1. Модель отвечает на чеченский промпт **по-чеченски** и по теме.
2. Нет систематических орфографических ошибок (палочка, диграфы).
3. Перплексия с адаптером ниже, чем у базы.
4. Ручная оценка носителя: ≥ 3.5 средний балл на ≥ 70 % ответов.
5. Воспроизводимость: `data/` + `configs/lora.yaml` + README → повторный запуск одной командой.

---

## 11. Результаты v0.1: обучение прошло, качество — нет [проверено]

### 11.1 Что получилось численно

Обучение отработало штатно: 1 800 итераций, 27 минут, пик памяти 7,1 ГБ, без ошибок.
Перплексия на `test.jsonl`:

| Модель | Test loss | Test ppl |
|---|---|---|
| база (нулевой адаптер) | 5,074 | 159,88 |
| чекпоинт 300 | 3,318 | 27,60 |
| чекпоинт 600 | 3,207 | 24,70 |
| чекпоинт 900 | 3,175 | 23,93 |
| **чекпоинт 1200** | **3,143** | **23,18** |
| чекпоинт 1500 | 3,211 | 24,82 |
| чекпоинт 1800 | 3,193 | 24,36 |

Падение перплексии **в 6,9 раза**. Но это метрика, а не качество.

### 11.2 Что получилось на самом деле

**Генерация деградировала.** Адаптер выдаёт повторы-циклы вместо перевода:

```
промпт:  Переведи на чеченский: Все живые существа на земле и птицы, летающие
         на двух крыльях, являются подобными вам сообществами...
адаптер: Цо элира цхьаьнанна тӀаккха тӀаккха тӀаккха тӀаккха тӀаккха тӀаккха...

промпт:  Переведи на чеченский: Мы не поступили с ними несправедливо...
адаптер: Цо эшам бу ца хӀуманна тӀехь тӀаккха а" ца хууш хууш хууш
```

**Ни один из трёх примеров из обучающей выборки модель не воспроизвела** — то есть она не
выучила даже то, на чём училась.

### 11.3 Диагностика — почему

Три независимых измерения.

**1. Базовая модель не знает чеченского.** Перплексия на одном и том же содержании
(выровненный `smol_ce`, 120 предложений):

| Язык | ppl |
|---|---|
| **чеченский** | **41,5** |
| русский | 8,3 |
| английский | 11,8 |

Чеченский в 5 раз хуже русского. На чеченский промпт база отвечает башкирско-казахской
абракадаброй (`Барыс һәм ҡаҙан ҡарашы`) — языка в модели просто нет.

**2. Модель условливается на промпт, но не выучила отображение.** Loss только на ответе,
верный промпт против чужого:

| Пример | верный промпт | чужой промпт | вывод |
|---|---|---|---|
| 1 | 1,820 | 2,391 | условливается |
| 2 | 2,391 | 3,078 | условливается |
| 3 | 2,062 | 4,875 | условливается |

Условливание есть, но loss **1,8–3,3 на примерах из обучения** — это провал: выученный
ответ должен давать около нуля. Модель застряла на «выучила общий вид чеченского текста».

**3. Ёмкости было мало.** rank 8, scale 20, 16 слоёв из 36 → **7,34 млн обучаемых
параметров (0,182 %)**. Для изучения языка с нуля этого мало.

### 11.4 Корневая причина

**Пропущен этап языковой адаптации.** Нельзя научить модель языку, которого она не знает,
с помощью 1 197 инструктивных пар и LoRA на 7 млн параметров. Инструктивная настройка
предполагает, что язык в модели уже есть — здесь его не было.

### 11.5 Найденный рычаг: ранг LoRA поднимается через YAML

`lora_parameters` не выведен в CLI (флага нет), но и не является argparse-аргументом,
поэтому в `main()` он всегда `None`, и значение из YAML **применяется**:

```yaml
lora_parameters:
  rank: 32
  scale: 64.0
```

| Конфиг | Обучаемых параметров |
|---|---|
| rank 8, num_layers 16 (v0.1) | 7,34 млн (0,182 %) |
| rank 32, num_layers 36 | **66,06 млн (1,642 %)** |

Это **в 9 раз больше ёмкости**, и без этого рычага проект упёрся бы в потолок.

### 11.6 Пересмотренный план: две стадии

| Стадия | Данные | Формат | Цель |
|---|---|---|---|
| **A. Языковая адаптация** | сырой чеченский текст: Коран, словарь, проза, `smol_ce`, `wmt24pp-ce` | `{"text": "..."}` | ppl на чеченском приблизить к русской (41,5 → ~12) |
| **B. Инструктивная настройка** | `train.jsonl` (chat) | `{"messages": [...]}` | поведение «ответь/переведи» |

Стадия A снимает главное препятствие. На ней регистр не важен — важен объём чеченского
текста, поэтому сюда можно брать и то, что отклонено для инструкций (например, Библию).

Гиперпараметры стадии A: `rank 32`, `num_layers 36`, `learning_rate 1e-4`,
`max_seq_length 512`, `batch_size 2`, формат `text`.

---

## Открытые вопросы [TBD]

1. **Целевой набор задач для v0.1** — только перевод ru↔ce, или перевод + 100–200
   рукописных инструкций? От этого зависит конструктор датасета.
2. Брать ли словную часть словаря (задача «перевод термина») или отложить?
3. Использовать ли стихи Библии — это другой регистр (архаичный), может испортить
   бытовой чеченский.

**Закрыто:** RAM (24 ГБ, бюджет MLX 17,8 ГБ); поддержка Qwen3 в `mlx-lm`
(есть `qwen3.py`); параметры модели (36 слоёв, 4 бита gs64); поведение chat-шаблона;
инвентарь источников; отсутствие чеченского в `google/wmt24pp`.
